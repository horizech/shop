import 'package:flutter/material.dart';

class AddEditAutoMobileProduct extends StatelessWidget {
  const AddEditAutoMobileProduct({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
